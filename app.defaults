[general]
key = string(default="")
secret = string(default="")
tweet_template = string(default="$user.screen_name$: $text$ $created_at$")
message_template = string(default="$sender.screen_name$: $text$ $created_at$")
soundpack = string(default="default")
soundvol = float(default=1)